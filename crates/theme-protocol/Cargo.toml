[package]
name = "retheme-theme-protocol"
version = "0.1.0"
edition = "2024"
license = "BSD-3-Clause"

[dependencies]
lightningcss = { version = "1.0.0-alpha.71", default-features = false }
semver = "1.0.28"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
zip = { version = "8.2.0", default-features = false, features = ["deflate"] }

[dev-dependencies]
tempfile = "3.23.0"

